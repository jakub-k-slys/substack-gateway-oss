[project]
name = "gateway_oss"
version = "0.0.3"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.135.1",
  "httpx>=0.28.0",
  "pydantic>=2.0",
  "pydantic-settings>=2.0",
  "async-lru>=2.0",
  "fastmcp>=3.0.2",
  "markdownify>=1.2.2",
  "uvloop>=0.22.1",
]

[project.scripts]
app = "gateway_oss.main:app"
gateway_oss = "gateway_oss.main:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/gateway_oss"]

[dependency-groups]
dev = [
  "ruff>=0.15.4",
  "ty>=0.0.19",
  "behave>=1.2.6",
  "respx>=0.22.0",
  "httpx>=0.28.0",
  "pytest>=8.0",
]

[tool.ruff]
line-length = 88
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
ignore = ["E501"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.behave]
paths = ["features/"]
show_timings = true
summary = true
default_format = "pretty"

[tool.pytest.ini_options]
testpaths = ["tests/"]

[tool.ty.src]
exclude = ["features/"]

[tool.semantic_release]
version_toml = ["pyproject.toml:project.version"]
branch = "main"
commit_author = "Jakub Slys <jakub@slys.dev>"
commit_message = "chore(release): {version}"
tag_format = "{version}"
