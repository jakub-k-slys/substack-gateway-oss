[project]
name = "gateway_notes_rest"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["gateway_core", "gateway_notes", "fastapi>=0.138.2"]

[project.entry-points."substack_gateway.capabilities"]
notes_rest = "gateway_notes_rest:capability"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/gateway_notes_rest"]

[tool.uv.sources]
gateway_core = { workspace = true }
gateway_notes = { workspace = true }
