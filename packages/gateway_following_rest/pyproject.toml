[project]
name = "gateway_following_rest"
version = "3.3.0"
requires-python = ">=3.11"
dependencies = ["gateway_core", "gateway_rest_common", "gateway_following", "fastapi>=0.141.1"]

[project.entry-points."substack_gateway.capabilities"]
following_rest = "gateway_following_rest:capability"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/gateway_following_rest"]

[tool.uv.sources]
gateway_core = { workspace = true }
gateway_rest_common = { workspace = true }
gateway_following = { workspace = true }
