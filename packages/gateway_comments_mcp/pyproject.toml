[project]
name = "gateway_comments_mcp"
version = "3.1.0"
requires-python = ">=3.11"
dependencies = ["gateway_core", "gateway_mcp_common", "gateway_comments", "fastmcp>=3.4.2"]

[project.entry-points."substack_gateway.capabilities"]
comments_mcp = "gateway_comments_mcp:capability"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/gateway_comments_mcp"]

[tool.uv.sources]
gateway_core = { workspace = true }
gateway_mcp_common = { workspace = true }
gateway_comments = { workspace = true }
